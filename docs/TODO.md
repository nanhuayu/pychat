# TODO List

## 功能改进

- [ ] 重要：增加 hook 插件，支持加入 feishu、telegram 等接口，直接通过消息接口注入 user 消息，触发 agent 执行；支持将消息发送到指定的 webhook URL，方便与第三方系统集成。
- [ ] 重要：支持 tui、cli 等多种界面形式，保持核心执行引擎和工具调用逻辑的复用。
- [ ] 重要：增加 Agent 模型编排，使得能支持类似 dify 和 coze 的多 agent 协同工作，完成复杂任务。并在 UI 中设计一个清晰的界面来展示多个 agent 的状态和交互。

### agent 改进：

- [X] 添加 multi-agent 支持（Chat、Agent、Plan 等多种角色协同工作）
- [ ] 重要：增加 sub-agent 功能（主 agent 可以根据需要临时创建 sub-agent 处理特定任务）
- [X] 针对 Plan 模式或者其它模式，增加 Plan 规划任务列表视图
- [X] 增加Agent 中途停止后续执行的功能（注入特殊命令继续执行，直到完成 attempt_completion 才算真正完成）
- [ ] 优化 Agent 的工具调用流程，增加调用前的确认步骤

### 工具能力改进：

- [X] 支持 SKILLS 加载和卸载（目前技能只能增量加载，无法卸载）
- [ ] 增加 SKILLS 技能商店功能（展示可用技能、安装/卸载技能等）
- [X] MCP 增加默认插件配置（开启或关闭默认插件）

- [X] 增加工具箱功能（如代码执行、文件操作等）
- [X] 增加记忆功能（短期记忆、长期记忆）
- [X] 增加权限管理功能（删除数据、敏感信息等）

- [ ] 增加工具调用结果的用户反馈机制（如选择是否继续执行、选择哪条路线等）
- [ ] 增加 ripgrep 作为搜索工具的支持
- [ ] 增加 shell 工具的会话管理功能，记录每个 shell 运行的所在窗口，方便后续进一步操作。

### 输入改进：

- [X] 统一 core.commands 命令解析和执行流程，支持 /plan、/skill xxx、/mode xxx 等命令，并且让它们都能进入正常的消息发送链，触发对应模式的运行。
- [X] 支持 ! 开头的 shell 命令，进入 shell 模式，执行系统命令并返回结果。
- [X] 支持 /plan <content> 命令直接进入 plan 模式，生成并执行计划，而不是停在命令层。
- [X] 支持 /{skill_name} 命令直接调用技能，而不是走单独的历史分支。

### 对话管理改进：

- [ ] 增加多模型接口支持（如 OpenAI Responses、Anthropic Responses、Ollama Chat 等）
- [X] 增加对话错误重试机制
- [ ] 增加对话总结功能（自动生成对话摘要，保留重要信息）
- [ ] 增加对话导出功能
- [ ] 增加附件上传功能（目前仅支持图片粘贴和拖放）
- [ ] 增加文件解析功能（如 PDF、Word、TXT、代码 等，其中PDF需要支持文本和图片两种内容的解析）
- [ ] 增加对话搜索功能


## 界面改进

- [X] 增加图标和品牌设计
- [X] 优化和设置右边栏（当前为统计信息，未来可扩展为插件市场等）
- [ ] 优化对话视图（如增加折叠、搜索等功能）
- [ ] 优化工具调用显示（如增加调用文件、代码块等的渲染）
- [ ] 增加多语言支持（当前仅支持中文）
- [X] 优化设置界面（如增加更多配置选项、更好的布局等）
- [X] 统一设置、对话和主窗口的主题风格
- [X] 支持 markdown 渲染（如代码块、表格等）
- [ ] 增加用户引导和帮助文档（如新手教程、常见问题等）


## 代码优化

- [X] 重构MCP管理模块
- [ ] 优化数据模型设计
- [X] 优化代理设置的应用逻辑
- [ ] 增加单元测试和集成测试覆盖率
- [X] 优化日志记录和错误处理机制
- [X] 拆分 UI 和 core 逻辑，减少耦合

## 发布

- [ ] 重要：基于 Nuitka 打包为 Windows 可执行文件（.exe）
- [ ] 发布代码到 GitHub 仓库
- [ ] 编写使用文档和示例
